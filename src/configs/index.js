const fs = require('fs');
const path = require('path');
const chalk = require('chalk');
const yargs = require('yargs');
const boxen = require('boxen');

module.exports = {
  fs,
  path,
  chalk,
  yargs,
  boxen,
};
